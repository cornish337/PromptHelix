class Prompt:
    """Represents a prompt with its versions."""
    # TODO: Define database schema for Prompt.
    # Example fields: id, name, description, created_at
    pass

class PromptVersion:
    """Represents a specific version of a prompt."""
    # TODO: Define database schema for PromptVersion.
    # Example fields: id, prompt_id, version_number, content, created_at, parent_version_id
    pass
